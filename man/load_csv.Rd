% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{load_csv}
\alias{load_csv}
\title{Register a CSV dataset as a view on a DuckDB connection}
\usage{
load_csv(conn, url, name, replace = TRUE, ...)
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{url}{Character scalar. Azure Blob URL. Supports glob patterns.}

\item{name}{Character scalar. Name to register the view under in DuckDB.}

\item{replace}{Logical. Replace an existing view. Default \code{TRUE}.}

\item{...}{Reader options forwarded to DuckDB's \code{read_csv_auto()}.}
}
\value{
Invisibly returns \code{conn}.
}
\description{
Validates the URL, loads the \code{azure} extension, then registers the dataset
as a VIEW over \code{read_csv_auto()}. Use \code{\link[=az_conn]{az_conn()}} first if the connection
needs an Azure secret. Returns \code{conn} invisibly — use \code{\link[=tbl_csv]{tbl_csv()}} if you
want a \code{dplyr::tbl()}.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
load_csv(conn, "abfss://container@account/data/*.csv", name = "events")
}
}
