% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{load_dataset}
\alias{load_dataset}
\title{Register a Delta, Parquet, CSV, or JSON dataset on a DuckDB connection}
\usage{
load_dataset(
  conn,
  url,
  name,
  format = c("delta", "parquet", "csv", "json"),
  ...
)
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{url}{Character scalar. Azure Blob URL.}

\item{name}{Character scalar. Name to register the dataset under in DuckDB.}

\item{format}{One of \code{"delta"}, \code{"parquet"}, \code{"csv"}, or \code{"json"}.}

\item{...}{Passed to the selected loader.}
}
\value{
Invisibly returns \code{conn}.
}
\description{
Dispatches to \code{\link[=load_delta]{load_delta()}}, \code{\link[=load_parquet]{load_parquet()}}, \code{\link[=load_csv]{load_csv()}}, or
\code{\link[=load_json]{load_json()}} based on \code{format}. Only arguments accepted by the target
function may be passed via \code{...}; passing \code{format}-incompatible arguments
raises an error.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
load_dataset(
  conn,
  "abfss://container@account/path/sales",
  name = "sales",
  format = "delta"
)
}
}
