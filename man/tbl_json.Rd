% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{tbl_json}
\alias{tbl_json}
\title{Open a JSON dataset as a lazy dplyr tbl}
\usage{
tbl_json(conn, url, name = NULL, replace = TRUE, ...)
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{url}{Character scalar. Azure Blob URL. Supports glob patterns.}

\item{name}{Optional character scalar. Name to register the view under in
DuckDB. When \code{NULL} (default) the dataset is scanned directly.}

\item{replace}{Logical. Replace an existing view of the same name.
Default \code{TRUE}. Ignored when \code{name = NULL}.}

\item{...}{Reader options forwarded to DuckDB's \code{read_json_auto()}.}
}
\value{
A \code{\link[dplyr:tbl]{dplyr::tbl()}} backed by the JSON dataset.
}
\description{
Validates the URL, loads the \code{azure} extension, then returns a lazy
\code{\link[dplyr:tbl]{dplyr::tbl()}} over the dataset. Use \code{\link[=az_conn]{az_conn()}} first if the connection
needs Azure extensions, settings, or secrets.
}
\details{
When \code{name} is \code{NULL} the dataset is queried directly via \code{read_json_auto()}
with no persistent object registered on the connection. When \code{name} is
supplied the dataset is first registered as a VIEW via \code{\link[=load_json]{load_json()}}, then
referenced by name.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
tbl_json(conn, "abfss://container@account/data/*.json") |>
  dplyr::collect()
}
}
