% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{tbl_delta}
\alias{tbl_delta}
\title{Open a Delta Lake table as a lazy dplyr tbl}
\usage{
tbl_delta(
  conn,
  url,
  name = NULL,
  method = c("attach", "view"),
  replace = TRUE,
  version = NULL,
  timestamp = NULL
)
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{url}{Character scalar. Azure Blob URL pointing to a Delta table
(e.g. \code{"abfss://container@account.dfs.core.windows.net/path/table"}).}

\item{name}{Optional character scalar. Name to register the table under in
DuckDB. When \code{NULL} (default) the table is scanned directly.}

\item{method}{\code{"attach"} (default) or \code{"view"}. Ignored when \code{name = NULL}.}

\item{replace}{Logical. Replace an existing registration of the same name.
Default \code{TRUE}. Ignored when \code{name = NULL}.}

\item{version}{Optional non-negative Delta table version to read.}

\item{timestamp}{Deprecated. Not supported: DuckDB's \code{delta} extension
accepts a \code{TIMESTAMP} attach option but ignores it, returning the latest
snapshot. Supplying it raises an error. Use \code{version} instead.}
}
\value{
A \code{\link[dplyr:tbl]{dplyr::tbl()}} backed by the Delta table.
}
\description{
Validates the URL, loads the \code{azure} and \code{delta} extensions, then returns a
lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the table. Use \code{\link[=az_conn]{az_conn()}} first if the connection
needs Azure extensions, settings, or secrets.
}
\details{
When \code{name} is \code{NULL} the table is queried directly via \code{delta_scan()} with
no persistent object registered on the connection. When \code{name} is supplied
the table is first registered via \code{\link[=load_delta]{load_delta()}} (as an ATTACH database or a
VIEW depending on \code{method}), then referenced by name.

Delta time travel currently requires \code{name} because DuckDB exposes
\code{version} through \code{ATTACH}, not \code{delta_scan()}.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
tbl_delta(conn, "abfss://container@account/path/sales") |>
  dplyr::filter(amount > 100) |>
  dplyr::collect()
}
}
