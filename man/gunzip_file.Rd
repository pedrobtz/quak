% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extensions.R
\name{gunzip_file}
\alias{gunzip_file}
\title{Decompress a gzip file to a destination path}
\usage{
gunzip_file(src, dest)
}
\arguments{
\item{src}{Character scalar. Path to the gzip-compressed source file.}

\item{dest}{Character scalar. Path to write the decompressed output to.}
}
\value{
Invisibly returns \code{dest}.
}
\description{
Streams \code{src} (a gzip-compressed file) through \code{\link[=gzfile]{gzfile()}} into \code{dest},
fully closing both connections before returning. Closing the output
connection flushes R's internal buffer to disk; skipping that step can
leave the trailing bytes — where a \code{.duckdb_extension} stores its metadata
footer — unwritten, yielding a corrupt file.
}
\keyword{internal}
