% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/azure.R
\name{az_conn}
\alias{az_conn}
\title{Open a DuckDB connection configured for Azure Data Lake Storage Gen2}
\usage{
az_conn(conn = NULL)
}
\arguments{
\item{conn}{An existing DuckDB connection to configure. When \code{NULL}
(default) a new in-memory connection is opened via \code{\link[=conn_open]{conn_open()}}.}
}
\value{
A DuckDB connection. The caller owns its lifetime; disconnect with
\code{DBI::dbDisconnect(conn, shutdown = TRUE)}.
}
\description{
Opens a DuckDB connection and installs the \code{azure} and \code{delta} extensions.
No secret is registered — use \code{\link[=az_set_token_secret]{az_set_token_secret()}}, \code{\link[=az_set_sp_secret]{az_set_sp_secret()}},
or \code{\link[=az_set_chain_secret]{az_set_chain_secret()}} to supply credentials afterwards.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn() |>
  az_set_token_secret(token = my_token)
DBI::dbDisconnect(conn, shutdown = TRUE)
}
}
