% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{tbl_parquet}
\alias{tbl_parquet}
\title{Open a Parquet dataset as a lazy dplyr tbl}
\usage{
tbl_parquet(conn, url, name = NULL, hive_partitioning = FALSE, replace = TRUE)
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{url}{Character scalar. Azure Blob URL. Supports glob patterns for
multi-file datasets
(e.g. \code{"abfss://container@account.dfs.core.windows.net/data/*.parquet"}).}

\item{name}{Optional character scalar. Name to register the view under in
DuckDB. When \code{NULL} (default) the dataset is scanned directly.}

\item{hive_partitioning}{Logical. Enable Hive partition inference from the
directory structure. Default \code{FALSE}.}

\item{replace}{Logical. Replace an existing view of the same name.
Default \code{TRUE}. Ignored when \code{name = NULL}.}
}
\value{
A \code{\link[dplyr:tbl]{dplyr::tbl()}} backed by the Parquet dataset.
}
\description{
Validates the URL, loads the \code{azure} extension, then returns a lazy
\code{\link[dplyr:tbl]{dplyr::tbl()}} over the dataset. Use \code{\link[=az_conn]{az_conn()}} first if the connection
needs Azure extensions, settings, or secrets.
}
\details{
When \code{name} is \code{NULL} the dataset is queried directly via \code{read_parquet()}
with no persistent object registered on the connection. When \code{name} is
supplied the dataset is first registered as a VIEW via \code{\link[=load_parquet]{load_parquet()}}, then
referenced by name. Glob patterns (e.g. \code{"*.parquet"}) are supported in
\code{url} for multi-file datasets.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
tbl_parquet(conn, "abfss://container@account/data/*.parquet") |>
  dplyr::collect()
}
}
