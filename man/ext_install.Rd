% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extensions.R
\name{ext_install}
\alias{ext_install}
\title{Install a DuckDB extension}
\usage{
ext_install(
  name,
  cache = ext_cache(),
  repo = c("core", "community"),
  conn = conn_default(),
  verbose = NULL
)
}
\arguments{
\item{name}{Character scalar. Extension name.}

\item{cache}{An \code{ext_cache} object used by the manual fallback.}

\item{repo}{\code{"core"} or \code{"community"}. Determines which configured URL to
use and, when no URL is set, which DuckDB install syntax to emit.}

\item{conn}{A DuckDB connection. Defaults to \code{\link[=conn_default]{conn_default()}}.}

\item{verbose}{Logical or \code{NULL}. When \code{TRUE}, emits a warning if the SQL
install fails but the manual fallback succeeds. When \code{NULL} (default),
uses the \code{quak.install_verbose} option / \code{QUAK_INSTALL_VERBOSE} env var.
When \code{FALSE}, the fallback is silent. Either way, a SQL failure is never
raised as an error on its own.}
}
\value{
Invisibly returns \code{conn}.
}
\description{
Tries two strategies in order, succeeding as soon as one works:
}
\details{
\enumerate{
\item \strong{SQL install:} runs DuckDB's built-in \code{INSTALL} (using the configured
repository URL when one is set via \code{\link[=repo_set_urls]{repo_set_urls()}}, the
\code{QUAK_CORE_REPO} / \code{QUAK_COMMUNITY_REPO} env vars, or the
\code{quak.core_repo} / \code{quak.community_repo} R options).
\item \strong{Manual fallback:} when the SQL install fails (e.g. DuckDB cannot
reach an HTTPS URL before \code{httpfs} is loaded, whereas R's \code{curl} can),
downloads the \code{.duckdb_extension} file, caches it, and copies it into
the extension directory.
}

A SQL failure is never raised on its own — it only surfaces (as a warning,
when \code{verbose = TRUE}) if the manual fallback also runs. An error is raised
only when both strategies fail.

Idempotent — skips install if the extension is already installed
(checked via the \code{duckdb_extensions()} pragma).
}
\examples{
\dontrun{
# Requires network access to download the extension.
conn <- DBI::dbConnect(duckdb::duckdb())
ext_install("httpfs", conn = conn)
DBI::dbDisconnect(conn, shutdown = TRUE)
}
}
