% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extensions.R
\name{ext_install_local}
\alias{ext_install_local}
\title{Install a DuckDB extension from a local file}
\usage{
ext_install_local(path, name = NULL, conn = conn_default())
}
\arguments{
\item{path}{Character scalar. Path to the \code{.duckdb_extension} file.}

\item{name}{Character scalar. Extension name used in messages. Inferred
from \code{path} when omitted.}

\item{conn}{A DuckDB connection. Defaults to \code{\link[=conn_default]{conn_default()}}.}
}
\value{
Invisibly returns \code{conn}.
}
\description{
Executes \verb{INSTALL '/path/to/ext.duckdb_extension'} on \code{conn}. Use this to
install an extension binary you already have on disk without going through a
remote repository.
}
\examples{
\dontrun{
# Requires a local DuckDB extension file at the given path.
conn <- DBI::dbConnect(duckdb::duckdb())
ext_install_local("/path/to/httpfs.duckdb_extension", conn = conn)
DBI::dbDisconnect(conn, shutdown = TRUE)
}
}
