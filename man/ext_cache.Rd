% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{ext_cache}
\alias{ext_cache}
\title{Extension cache}
\usage{
ext_cache(cache_path = ext_cache_path())
}
\arguments{
\item{cache_path}{Character scalar. Cache root directory. Defaults to
\code{\link[=ext_cache_path]{ext_cache_path()}}.}
}
\value{
An \code{ext_cache} object (a list of closures) with elements:
\itemize{
\item \code{.path}: the cache root.
\item \code{get(name, version, platform)}: path to the cached extension, or \code{NULL}.
\item \code{add(name, version, platform, src)}: copies \code{src} into the cache.
\item \code{list()}: data frame of cached extensions.
\item \code{del(name, version, platform)}: removes a cached extension. When \code{version}
and \code{platform} are omitted, removes all cached entries for \code{name}.
}
}
\description{
Builds an \code{ext_cache} object: a list of closures bound to a cache directory,
implementing CRUD over cached \code{.duckdb_extension} files. Files are laid out
under \verb{<cache_path>/<version>/<platform>/<name>.duckdb_extension}.
}
\examples{
cache <- ext_cache(file.path(tempdir(), "quak-cache"))
cache$.path
}
