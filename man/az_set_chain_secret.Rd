% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/azure.R
\name{az_set_chain_secret}
\alias{az_set_chain_secret}
\title{Register an Azure credential-chain secret}
\usage{
az_set_chain_secret(conn, account = NULL, chain = "default")
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{account}{Optional storage account name. When supplied, the secret is
scoped to that account.}

\item{chain}{Optional character vector of DuckDB credential-chain entries.
Values are joined with semicolons and passed as DuckDB's \code{CHAIN} value.
Defaults to \code{"default"}, DuckDB's default credential chain.}
}
\value{
Invisibly returns \code{conn}.
}
\description{
Creates or replaces a DuckDB Azure secret using the \code{credential_chain}
provider. This lets DuckDB resolve credentials itself, for example from the
Azure CLI or environment.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
az_set_chain_secret(conn, chain = "cli")
}
}
