% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake.R
\name{az_schema}
\alias{az_schema}
\title{Inspect a dataset schema without collecting data}
\usage{
az_schema(conn, url, format = NULL)
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{url}{Character scalar. Azure Blob URL.}

\item{format}{Optional format override. One of \code{"parquet"}, \code{"csv"},
\code{"json"}, or \code{"delta"}. When \code{NULL}, inferred from \code{url}.}
}
\value{
A tibble-like data frame with columns \code{name} and \code{type}.
}
\description{
Uses DuckDB's \verb{DESCRIBE SELECT} over a remote scan and returns only column
names and DuckDB types.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
az_schema(conn, "abfss://container@account/data/*.parquet")
}
}
