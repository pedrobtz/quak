% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{load_delta}
\alias{load_delta}
\title{Register a Delta Lake table on a DuckDB connection}
\usage{
load_delta(
  conn,
  url,
  name,
  method = c("attach", "view"),
  replace = TRUE,
  version = NULL,
  timestamp = NULL
)
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{url}{Character scalar. Azure Blob URL pointing to a Delta table.}

\item{name}{Character scalar. Name to register the table under in DuckDB.}

\item{method}{\code{"attach"} (default) or \code{"view"}.}

\item{replace}{Logical. Replace an existing registration. Default \code{TRUE}.}

\item{version}{Optional non-negative Delta table version to attach.}

\item{timestamp}{Optional Delta table timestamp to attach. Only one of
\code{version} and \code{timestamp} may be supplied.}
}
\value{
Invisibly returns \code{conn}.
}
\description{
Validates the URL, loads the \code{azure} and \code{delta} extensions, then registers
the table either as an ATTACH database or a VIEW. Use \code{\link[=az_conn]{az_conn()}} first if
the connection needs an Azure secret. Returns \code{conn} invisibly — use
\code{\link[=tbl_delta]{tbl_delta()}} if you want a \code{dplyr::tbl()}.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
load_delta(conn, "abfss://container@account/path/sales", name = "sales")
DBI::dbGetQuery(conn, "SELECT COUNT(*) FROM sales")
}
}
