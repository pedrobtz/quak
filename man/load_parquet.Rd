% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{load_parquet}
\alias{load_parquet}
\title{Register a Parquet dataset as a view on a DuckDB connection}
\usage{
load_parquet(conn, url, name, hive_partitioning = FALSE, replace = TRUE)
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{url}{Character scalar. Azure Blob URL. Supports glob patterns.}

\item{name}{Character scalar. Name to register the view under in DuckDB.}

\item{hive_partitioning}{Logical. Enable Hive partition inference. Default \code{FALSE}.}

\item{replace}{Logical. Replace an existing view. Default \code{TRUE}.}
}
\value{
Invisibly returns \code{conn}.
}
\description{
Validates the URL, loads the \code{azure} extension, then registers the dataset
as a VIEW. Use \code{\link[=az_conn]{az_conn()}} first if the connection needs an Azure secret.
Returns \code{conn} invisibly — use \code{\link[=tbl_parquet]{tbl_parquet()}} if you want a \code{dplyr::tbl()}.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
load_parquet(conn, "abfss://container@account/data/*.parquet", name = "events")
}
}
