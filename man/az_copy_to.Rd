% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake.R
\name{az_copy_to}
\alias{az_copy_to}
\title{Copy data to Azure Data Lake Storage Gen2}
\usage{
az_copy_to(
  conn,
  x,
  url,
  format = c("parquet", "csv", "json"),
  partition_by = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{x}{A lazy \code{dbplyr} table, data frame, SQL string, or \code{DBI::SQL}
object.}

\item{url}{Character scalar. Azure Blob URL to write to.}

\item{format}{Output format. One of \code{"parquet"}, \code{"csv"}, or \code{"json"}.}

\item{partition_by}{Optional character vector of columns to partition by.}

\item{overwrite}{Logical. When \code{TRUE}, passes DuckDB's
\code{OVERWRITE_OR_IGNORE} copy option.}
}
\value{
Invisibly returns \code{url}.
}
\description{
Writes a lazy table, data frame, or SQL query to an \verb{abfs://} or
\verb{abfss://} URL using DuckDB's \verb{COPY ... TO} command.
}
\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
az_copy_to(
  conn,
  "SELECT * FROM events WHERE event_date >= DATE '2026-01-01'",
  "abfss://container@account/exports/events",
  format = "parquet"
)
}
}
