% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/azure.R
\name{az_list_secrets}
\alias{az_list_secrets}
\title{List Azure secrets registered in DuckDB}
\usage{
az_list_secrets(conn = conn_default())
}
\arguments{
\item{conn}{A DuckDB connection. Defaults to \code{\link[=conn_default]{conn_default()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the columns returned by
\code{duckdb_secrets()}.
}
\description{
Queries \code{duckdb_secrets()} and returns secrets whose \code{type} is \code{"azure"}.
Values are returned as DuckDB reports them; DuckDB handles redaction of
sensitive fields.
}
\examples{
conn <- DBI::dbConnect(duckdb::duckdb())
az_list_secrets(conn)
DBI::dbDisconnect(conn, shutdown = TRUE)
}
