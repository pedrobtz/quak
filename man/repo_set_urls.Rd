% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repositories.R
\name{repo_set_urls}
\alias{repo_set_urls}
\title{Set DuckDB extension repository URLs}
\usage{
repo_set_urls(
  core = NULL,
  community = NULL,
  check = TRUE,
  conn = conn_default()
)
}
\arguments{
\item{core}{Optional character scalar. URL for the core extension repository.
Omit to leave the current value unchanged. Pass \code{NULL} to reset to the
DuckDB default.}

\item{community}{Optional character scalar. URL for the community extension
repository. Omit to leave the current value unchanged. Pass \code{NULL} to
reset to the DuckDB default.}

\item{check}{Logical. If \code{TRUE} (default), calls \code{\link[=repo_check]{repo_check()}} for each
repository whose URL was changed, probing \code{"httpfs"} as a baseline
extension.}

\item{conn}{A DuckDB connection. Defaults to \code{\link[=conn_default]{conn_default()}}. Used to set
\code{custom_extension_repository} when \code{core} is supplied, and by
\code{\link[=repo_check]{repo_check()}} when \code{check = TRUE}.}
}
\value{
Invisibly returns a named list with elements \code{core} and \code{community}
reflecting the current option values.
}
\description{
Stores URLs in R options \code{quak.core_repo} / \code{quak.community_repo} so they
can be configured org-wide in \code{.Rprofile}. When \code{core} is supplied, also
sets DuckDB's \code{custom_extension_repository} on \code{conn}; passing \code{NULL}
resets that connection setting to DuckDB's default.
}
\examples{
old <- repo_urls()
repo_set_urls(core = "https://extensions.example.com", check = FALSE)
repo_urls()
repo_set_urls(core = old$core, check = FALSE)
}
