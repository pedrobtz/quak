% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arrow.R
\name{stream_arrow}
\alias{stream_arrow}
\title{Stream an Azure-backed lazy tbl as Arrow record batches}
\usage{
stream_arrow(x, chunk_size = 1e+06)
}
\arguments{
\item{x}{A \code{tbl_az} produced by \code{\link[=tbl_delta]{tbl_delta()}}, \code{\link[=tbl_parquet]{tbl_parquet()}},
\code{\link[=tbl_csv]{tbl_csv()}} or \code{\link[=tbl_json]{tbl_json()}}.}

\item{chunk_size}{Positive whole number. The maximum number of rows in
each batch.}
}
\value{
A \code{nanoarrow_array_stream}.
}
\description{
Runs the query behind a table created by \code{\link[=tbl_delta]{tbl_delta()}}, \code{\link[=tbl_parquet]{tbl_parquet()}},
\code{\link[=tbl_csv]{tbl_csv()}} or \code{\link[=tbl_json]{tbl_json()}} and returns a stream that yields the result
in batches of at most \code{chunk_size} rows. Batches are produced as they are
read, so the whole result never needs to fit in memory. Runs the same
pre-flight checks as \code{\link[=collect.tbl_az]{collect.tbl_az()}}.
}
\details{
Requires the \pkg{nanoarrow} package. Read batches with
\code{stream$get_next()}, which returns \code{NULL} once the stream is exhausted,
or pass the stream to \code{arrow::as_record_batch_reader()}.
}
\section{Reading the stream}{

Read the stream to the end before running any other query on the same
connection. DuckDB ends an open stream as soon as the connection runs
another query, and the stream then reports that it is exhausted without
an error, so the remaining rows are lost silently.

To stop reading early, call \code{stream$release()}. It frees the query and
its buffers at once, rather than when R next collects garbage.
}

\examples{
\dontrun{
# Requires a live Azure account, credentials, and network access.
conn <- az_conn()
sales <- tbl_delta(conn, "abfss://container@account/path/sales")
stream <- stream_arrow(sales, chunk_size = 100000)
while (!is.null(batch <- stream$get_next())) {
  message(batch$length, " rows")
}
}
}
\seealso{
\code{\link[=collect_arrow]{collect_arrow()}} to read the whole result at once.
}
