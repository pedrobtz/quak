% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{conn_setting}
\alias{conn_setting}
\title{Get or set DuckDB settings}
\usage{
conn_setting(conn = conn_default(), name = NULL, value = NULL)
}
\arguments{
\item{conn}{A DuckDB connection.}

\item{name}{Optional character scalar. Setting name.}

\item{value}{Optional value to set. Coerced to character; DuckDB casts it to
the appropriate type.}
}
\value{
All settings: a \code{\link[tibble:tibble]{tibble::tibble()}}. Single setting read: a
character scalar. Write: \code{conn} invisibly.
}
\description{
When called with no arguments, returns all settings as a data frame. When \code{name}
is supplied and \code{value} is \code{NULL}, returns the value of that setting. When
both \code{name} and \code{value} are supplied, executes \verb{SET <name> = <value>}.
}
\examples{
conn <- DBI::dbConnect(duckdb::duckdb())
conn_setting(conn, "threads")
DBI::dbDisconnect(conn, shutdown = TRUE)
}
